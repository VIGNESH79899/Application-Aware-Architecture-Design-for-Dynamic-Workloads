{
    "name": "coa-project",
    "version": "1.0.0",
    "scripts": {
        "install-all": "npm install && cd frontend && npm install && cd .. && pip install -r backend/requirements.txt",
        "dev": "concurrently \"python backend/app.py\" \"cd frontend && npm run dev\"",
        "start": "npm run dev"
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}